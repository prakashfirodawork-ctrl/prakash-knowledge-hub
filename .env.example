# Database — required for API server
DATABASE_URL=postgresql://user:password@localhost:5432/prakash_hub

# Optional: set when frontend and API are deployed on separate domains
# VITE_API_BASE_URL=https://your-api-server.com

# Session secret — required for API server sessions
SESSION_SECRET=change-me-to-a-long-random-string
